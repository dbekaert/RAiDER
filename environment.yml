# create environment : conda env create -f environment.yml
# update dependencies: conda env update -f environment.yml
# remove environment : conda env remove -n RAiDER
# enter  environment : conda activate RAiDER
# exit   environment : conda deactivate
name: RAiDER
channels:
 - conda-forge
 - nodefaults
dependencies:
 - python>=3.8,<3.14
 - pip>=25,<26
 # For running
 - asf_search>=9,<10
 - boto3>=1,<2
 - cartopy<1
 - cdsapi<1
 - cfgrib<1
 - dask
 - dem_stitcher>=2.5.8,<3
 - ecmwf-api-client>=1,<2
 - h5netcdf>=1,<2
 - h5py>=2,<4
 - herbie-data<2025.2.1
 - isce3>=0.15.0,<1
 - jsonschema>=3.2.0,<4  # this is for ASF DAAC ingest schema validation
 - lxml>=6,<7
 - matplotlib>=3,<4
 - netcdf4>=1,<2
 - numpy>=1,<3
 - pandas>=2,<3
 - progressbar>=2,<3
 - pydap>=3.2.3,<4
 - pyproj>=2.2.0,<4
 - pyyaml>=6,<7
 - rasterio>=1.3.0,<2
 - requests>=2,<3
 - rioxarray<1
 - s3fs
 - scipy>=1.10.1,<2
 - s1_orbits<1
 - shapely>=2,<3
 - tqdm>=4,<5
 - xarray
 # For packaging and testing
 - autopep8>=2,<3
 - cmake>=4,<5
 - cxx-compiler>=1,<2
 - cython>=3,<4
 - pybind11>=2,<3
 - pytest>=8,<9
 - pytest-console-scripts>=1,<2
 - pytest-cov>=6,<7
 - pytest-mock>=3,<4
 - pytest-timeout>=2,<3
 - setuptools_scm>=6.2,<9
 - sysroot_linux-64>=2,<3
 # For docs website
 - mkdocs>=1,<2
 - mkdocs-macros-plugin>=1,<2
 - mkdocs-material>=9,<10
 - mkdocs-material-extensions>=1,<2
 - mkdocstrings<1
 - mkdocstrings-python>=1,<2
 # For RAiDER-docs
 - jupyter_contrib_nbextensions<1
 - jupyterlab>=3,<4
 - wand<1
 # For development
 - boto3-stubs>=1,<2
